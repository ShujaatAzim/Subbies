import React from 'react';

const NavBar = () => {
  return (
    <nav>
      <button>Home</button>
      <button>Properties</button>
      <button>Logout</button>
    </nav>
  );
}

export default NavBar;